mutation ($projectPath: ID!, $assigneeUsernames: [String!]!, $iid: String!) {
    setAlertAssignees(input: { iid: $iid, assigneeUsernames: $assigneeUsernames, projectPath: $projectPath }) {
        errors
        alert {
            iid,
            assignees {
                username
            },
        }
    }
}
