- page_title _("Iterations")

.js-iterations-list{ data: { group_full_path: @group.full_path, can_admin: can?(current_user, :create_iteration, @group).to_json, new_iteration_path: new_group_iteration_path(@group) } }
