mutation($input: JiraImportStartInput!) {
  jiraImportStart(input: $input) {
    clientMutationId
    jiraImport {
      jiraProjectKey
      scheduledAt
      scheduledBy {
        name
      }
    }
    errors
  }
}
