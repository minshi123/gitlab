
.lsif:
  stagge: lsif
  allow_failure: true
  artifacts:
    reports:
      lsif: dump.lsif
  rules:
    - if: '$LSIF_DISABLED'
      when: never

lsif-go:
  extends: .lsif
  image: golang:1.14.0
  script:
    - go get github.com/sourcegraph/lsif-go/cmd/lsif-go
    - lsif-go
