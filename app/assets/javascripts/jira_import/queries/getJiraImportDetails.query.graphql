query($fullPath: ID!) {
  project(fullPath: $fullPath) {
    jiraImportStatus
    jiraImports(last: 1) {
      nodes {
        jiraProjectKey
        scheduledAt
        scheduledBy {
          name
        }
      }
    }
  }
}
